<%= form_for([document, suggestion, suggestion_user_comment]) do |f| %>
  <% if suggestion_user_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suggestion_user_comment.errors.count, "error") %> prohibited this suggestion_user_comment from being saved:</h2>

      <ul>
      <% suggestion_user_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.hidden_field(:user_id, :value => current_user.id) %>
  </div>

  <div class="field">
    <%= f.hidden_field(:suggestion_id, :value => params[:id]) %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
